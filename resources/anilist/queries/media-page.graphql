query MediaPage($page: Int, $perPage: Int) {
  Page(page: $page, perPage: $perPage) {
    pageInfo {
      currentPage
      lastPage
      hasNextPage
    }
    media {
      id
      type
      format
      status
      title {
        romaji
        english
        native
      }
      synonyms
      episodes
      chapters
      averageScore
      coverImage {
        medium
        large
        extraLarge
      }
      description
    }
  }
}
